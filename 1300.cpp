#include <iostream>
#include <cmath>
template<typename T>
inline T& min(T& lhs, T& rhs){return lhs > rhs ? rhs: lhs;}
template<typename T>
inline T& max(T& lhs, T& rhs){return lhs < rhs ? rhs: lhs;}
using namespace std;
int main()
{
    int N = 0;
    cin >> N;
    int k = 0;
    cin >> k;
    long long last = 0;

}
